with sim;
with Ada.Real_Time; use Ada.Real_Time;
with Ada.Text_IO;

--@Author: Piotr Olejarz 220398
--Main procedure just invokes the simulation
procedure Main is
  -- ts1,ts2 : Time_Span;
  -- sc1,sc2 : Seconds_Count;
  -- f : float;

begin
   --Split(T => Clock,SC => sc1,TS => ts1);
  -- delay Standard.Duration(1.5);
  -- Split(T => Clock,SC => sc2,TS => ts2);
   --ts2 := ts2-ts1;
   --sc2 := sc2-sc1;

  -- Ada.Text_IO.Put_Line("seconds:"&Seconds_Count'Image(sc2));
  -- Ada.Text_IO.Put_Line("seconds:"&Duration'Image(To_Duration(ts2)));
  -- f := Float'Value(Seconds_Count'Image(sc2)) + Float'Value(Duration'Image(To_Duration(ts2))) ;
  -- Ada.Text_IO.Put_Line("seconds:"&Float'Image(f));
   sim.simulation_start;



end Main;
